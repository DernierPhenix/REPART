{% extends 'base.html.twig' %}

{% block title %}Modification Categorie
{% endblock %}


{% block contenu %}
	<div class="container">
		<h1>Modification Categorie</h1>
		<a class='btn btn-primary' href="{{ path('app_categories_index', {'id': category.id}) }}">Retour Liste</a><br>
		<tbody class="d-flex justify-content-center ">
			{{ include('categories/_form.html.twig', {'button_label': 'Modifier'}) }}
			<br>
			{{ include('categories/_delete_form.html.twig') }}
		</tbody>
	</div>
{% endblock %}
