{% extends 'base.html.twig' %}

{% block title %}Catégories
{% endblock %}

{% block contenu %}
	<div class="container">
		<br/>
		<h1>Categorie</h1>

   

    
<a class='btn btn-primary' href="{{ path('app_categories_index', {'id': category.id}) }}">Retour Liste</a> <br> <br>
<a class='btn btn-primary' href="{{ path('app_categories_edit', {'id': category.id}) }}">Modifier</a><br> <br>
    {{ include('categories/_delete_form.html.twig') }}
{% endblock %}
