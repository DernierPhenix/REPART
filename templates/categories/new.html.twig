{% extends 'base.html.twig' %}

{% block title %}Création Catégories{% endblock %}

{% block contenu %}
    <h1>Créer une Nouvelle Catégorie</h1>

    {{ include('categories/_form.html.twig') }}

   <a class='btn btn-primary' id='btn-cat' href="{{ path('app_categories_index', {'id': category.id}) }}">Retour Liste</a><br>
    <br>
{% endblock %}
