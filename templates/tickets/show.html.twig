{% extends 'base.html.twig' %}

{% block title %}Tickets{% endblock %}

{% block contenu %}
    <h1>Tickets</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ticket.id }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ ticket.etat }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ ticket.statut }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ ticket.description }}</td>
            </tr>
            <tr>
                <th>Date de création</th>
                <td>{{ ticket.createdAt ? ticket.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Date de modification</th>
                <td>{{ ticket.updatedAt ? ticket.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Rapport</th>
                <td>{{ ticket.rapport }}</td>
            </tr>
             <tr>
                <th>Employe</th>
                <td>{{ ticket.user.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_tickets_index') }}">Retour liste</a>

    <a class="btn btn-primary" href="{{ path('app_tickets_edit', {'id': ticket.id}) }}">Modifier</a><br><br>

    {{ include('tickets/_delete_form.html.twig') }}
{% endblock %}
