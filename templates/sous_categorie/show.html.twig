{% extends 'base.html.twig' %}

{% block title %}SousCategorie{% endblock %}

{% block contenu %}
    <h1>SousCategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sous_categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ sous_categorie.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sous_categorie_index') }}"class="btn btn-primary">Retour liste</a>

    <a href="{{ path('app_sous_categorie_edit', {'id': sous_categorie.id}) }}"class="btn btn-primary">Modifier</a>

    {{ include('sous_categorie/_delete_form.html.twig') }}
{% endblock %}
